import Image from "next/image";
import { useState } from "react";

export default function Home() {
  // Dark Mode Umschaltung
  const [darkMode, setDarkMode] = useState(false);

  return (
    <div className={`min-h-screen ${darkMode ? "bg-black text-white" : "bg-zinc-50 text-black"} transition-all`}>
      <header className="p-4 flex justify-between items-center max-w-7xl mx-auto">
        <h1 className="text-3xl font-bold">Rasel's Portfolio</h1>
        <button
          onClick={() => setDarkMode(!darkMode)}
          className="px-4 py-2 rounded-full bg-blue-500 text-white dark:bg-blue-700 dark:text-black transition-colors"
        >
          {darkMode ? "Light Mode" : "Dark Mode"}
        </button>
      </header>

      <main className="flex flex-col items-center justify-center py-16 sm:py-32">
        <Image
          src="/your-profile-pic.jpg" // Dein Profilbild
          alt="Rasel"
          width={150}
          height={150}
          className="rounded-full mb-6"
        />
        <h2 className="text-4xl font-semibold mb-4">Hi, I'm Rasel!</h2>
        <p className="text-xl text-center max-w-2xl mb-6">
          Ich bin ein Informatikstudent und leidenschaftlicher Entwickler. Ich programmiere, um die Welt mit Technologie zu verbessern. Hier ist mein Portfolio!
        </p>

        {/* Buttons für Portfolio und Projekte */}
        <div className="flex gap-6">
          <a
            href="#portfolio"
            className="px-6 py-3 bg-blue-600 text-white rounded-lg shadow-lg transition-all hover:bg-blue-700"
          >
            Portfolio
          </a>
          <a
            href="#projects"
            className="px-6 py-3 border-2 border-blue-600 text-blue-600 rounded-lg shadow-lg transition-all hover:bg-blue-600 hover:text-white"
          >
            Meine Projekte
          </a>
        </div>
      </main>

      {/* Portfolio Section */}
      <section id="portfolio" className="py-16 bg-zinc-100 dark:bg-black dark:text-white">
        <div className="text-center">
          <h3 className="text-3xl font-semibold mb-4">Mein Portfolio</h3>
          <p className="text-xl mb-6">Hier sind einige meiner besten Arbeiten:</p>

          {/* Beispiel für ein Projekt */}
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            <div className="bg-white p-6 rounded-lg shadow-lg transition-all hover:shadow-xl">
              <Image
                src="/project1.png"
                alt="Projekt 1"
                width={300}
                height={200}
                className="rounded-t-lg"
              />
              <h4 className="text-xl font-semibold mt-4">Projekt 1</h4>
              <p className="text-md text-zinc-600 dark:text-zinc-300">Beschreibung des Projekts 1.</p>
            </div>
            <div className="bg-white p-6 rounded-lg shadow-lg transition-all hover:shadow-xl">
              <Image
                src="/project2.png"
                alt="Projekt 2"
                width={300}
                height={200}
                className="rounded-t-lg"
              />
              <h4 className="text-xl font-semibold mt-4">Projekt 2</h4>
              <p className="text-md text-zinc-600 dark:text-zinc-300">Beschreibung des Projekts 2.</p>
            </div>
            {/* Weitere Projekte hier */}
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-zinc-800 text-white text-center py-6 mt-16">
        <p>&copy; 2026 Rasel. Alle Rechte vorbehalten.</p>
        <div className="space-x-4">
          <a href="https://github.com/rasel" target="_blank" rel="noopener noreferrer">GitHub</a>
          <a href="https://linkedin.com/in/rasel" target="_blank" rel="noopener noreferrer">LinkedIn</a>
        </div>
      </footer>
    </div>
  );
}
